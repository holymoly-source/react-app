import { createContext } from "react";
export const ContextApi = createContext();

export const initialState = {
  basket: [
    {
      name: "Manash",
    },
    {
      name: "Akash",
    },
  ],
};
